#include <stdio.h>
#include "read.h"
#include "version.h"
#include "act.h"
#include <string.h>
int read()
{
/*v[0].id[0].a_id=500;
strcpy(v[0].id[0].state,"Committed");
strcpy(act[0].state,"Comm");
return 0;*/
}
